import PlaceholderPage from "@/components/placeholder-page";

export default function AssetsPage() {
  return (
    <PlaceholderPage
      title="Asset Directory"
      description="Register, search, and track assets through their full lifecycle."
    />
  );
}
