import PlaceholderPage from "@/components/placeholder-page";

export default function AllocationsPage() {
  return (
    <PlaceholderPage
      title="Allocations & Transfers"
      description="Allocate assets with conflict handling and transfer workflows."
    />
  );
}
