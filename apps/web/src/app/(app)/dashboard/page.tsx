import PlaceholderPage from "@/components/placeholder-page";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="KPIs, overdue returns, and quick actions for every role."
    />
  );
}
